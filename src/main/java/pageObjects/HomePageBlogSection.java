package pageObjects;

import org.openqa.selenium.WebDriver;
import classesUtilities.Page;

public class HomePageBlogSection extends Page {
    public HomePageBlogSection(WebDriver driver) {
        super(driver);
    }
}
